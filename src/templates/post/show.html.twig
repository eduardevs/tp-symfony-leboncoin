{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}

    <link href="{{ asset('assets/css/post.css') }}" rel="stylesheet"/>

    <h1>Post</h1>

    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ post.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ post.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ post.price }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ post.date ? post.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table> #}

    <div class="row mb-3">

        <a href="{{ path('app_post_index') }}" class="retour">&#8592 Retour</a>
        
    </div>

    <div class="row">

        <div class="d-flex row m-1">
            <div class="col card">

                <div id="carouselControls" class="carousel slide" data-bs-ride="carousel">
                    <div class="carousel-inner">
                    {% for image in post.images %}

                        <div class="carousel-item">
                            {# <img class="d-block w-100" src={{post.images.link}} alt="Photo" > #}
                            <img class="d-block w-100" src={{image}} alt="Photo">
                        </div>

                        {# <div class="carousel-item">
                            <img class="d-block w-100" src={{image}} alt="Photo">
                        </div> #}
                        {# <div class="carousel-item">
                            <img class="d-block w-100" src="https://images.unsplash.com/photo-1665516533754-d98825ae07e6?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHx0b3BpYy1mZWVkfDMxfFJfRnluLUd3dGx3fHxlbnwwfHx8fA%3D%3D&auto=format&fit=crop&w=500&q=60" alt="Photo">
                        </div> #}
                    {% endfor %}
                    </div>
                    <a class="carousel-control-prev" href="#carouselControls" role="button" data-bs-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselControls" role="button" data-bs-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            
            </div>

            <div class="col">

                <div class="col card mb-3 p-3 shadow">

                    <span class="name">{{ post.name }}</span>

                    <span>{{ post.price }} €</span>
                    
                    <span>Date de publication : {{ post.date ? post.date|date('d/m/Y') : '' }}</span>

                </div>

                <div class="col card mt-4 p-3 shadow">

                    <span class="description">Description</span>
                    
                    <span> {{ post.description }}</span>

                </div>
        
        
            </div>
    
        </div>
        
    </div>

    <div class="row justify-content-end mt-3">

        <div class="col-auto">

            <a href="{{ path('app_post_edit', {'id': post.id}) }}"  class="btn btn-outline-primary">Modifier</a>

        </div>

        <div class="col-auto">

            {{ include('post/_delete_form.html.twig') }}

        </div>
        
    </div>

<script src="{{ asset('assets/js/post.js') }}"></script>
{% endblock %}

